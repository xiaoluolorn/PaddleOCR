% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdf.R
\name{pdf_to_markdown_with_paddle}
\alias{pdf_to_markdown_with_paddle}
\title{Convert a PDF to Markdown via PaddleOCR}
\usage{
pdf_to_markdown_with_paddle(
  pdf_path,
  output_dir = NULL,
  combined_markdown = TRUE,
  dpi = 300,
  batch_trigger = 3,
  workers = 1,
  resume = TRUE,
  token = "",
  job_url = "",
  model = "",
  use_doc_orientation_classify = FALSE,
  use_doc_unwarping = FALSE,
  use_chart_recognition = FALSE,
  poll_interval = 5,
  max_wait_seconds = 1800,
  timeout = 600,
  ...
)
}
\arguments{
\item{pdf_path}{Path to the PDF file.}

\item{output_dir}{Output directory. If \code{NULL}, defaults to
\code{"<pdf_name>_paddle_output"} next to the PDF.}

\item{combined_markdown}{Logical; if \code{TRUE} (default), combine all
page Markdown into a single file.}

\item{dpi}{Image resolution for rendering (default: 300).}

\item{batch_trigger}{Number of pages to render before starting OCR
(default: 3).}

\item{workers}{Maximum number of OCR jobs submitted concurrently (default:
1). OCR runs concurrently on the PaddleOCR service; result polling remains
local and sequential.}

\item{resume}{Logical; reuse rendered page images, completed Markdown files,
and submitted job IDs from an interrupted run (default: \code{TRUE}).}

\item{token}{PaddleOCR API token.}

\item{job_url}{PaddleOCR API endpoint.}

\item{model}{Model name.}

\item{use_doc_orientation_classify}{Logical; enable orientation classification.}

\item{use_doc_unwarping}{Logical; enable document unwarping.}

\item{use_chart_recognition}{Logical; enable chart recognition.}

\item{poll_interval}{Polling interval in seconds.}

\item{max_wait_seconds}{Maximum wait time per job.}

\item{timeout}{HTTP timeout.}

\item{...}{Ignored (for future compatibility).}
}
\value{
A list with PDF path, image paths, Markdown file paths, combined
Markdown path and text, and output directory.
}
\description{
Renders PDF pages to images one by one, then submits them for OCR using
the PaddleOCR cloud API. Pages are processed in streaming batches:
once \code{batch_trigger} pages are rendered, OCR begins while rendering
continues.
}
\details{
Requires the \pkg{pdftools} package.
}
\examples{
\dontrun{
result <- pdf_to_markdown_with_paddle("document.pdf")
cat(result$combined_markdown_text)
}
}
