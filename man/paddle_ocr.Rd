% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paddle_ocr.R
\name{paddle_ocr}
\alias{paddle_ocr}
\title{Perform OCR on an image, PDF, or URL using PaddleOCR}
\usage{
paddle_ocr(
  file_path,
  output_dir = "output",
  token = "",
  job_url = "",
  model = "",
  use_doc_orientation_classify = FALSE,
  use_doc_unwarping = FALSE,
  use_chart_recognition = FALSE,
  poll_interval = 5,
  max_wait_seconds = 3600,
  timeout = 600
)
}
\arguments{
\item{file_path}{A local file path or a URL (\code{http://} or
\code{https://}).}

\item{output_dir}{Directory to save output Markdown and images
(default: \code{"output"}).}

\item{token}{PaddleOCR API bearer token. If empty, reads from the
\code{PADDLE_OCR_TOKEN} environment variable.}

\item{job_url}{PaddleOCR API endpoint. Defaults to the official cloud URL.}

\item{model}{Model name to use. Defaults to \code{"PaddleOCR-VL-1.6"}.}

\item{use_doc_orientation_classify}{Logical; enable document orientation
classification.}

\item{use_doc_unwarping}{Logical; enable document unwarping.}

\item{use_chart_recognition}{Logical; enable chart/table recognition.}

\item{poll_interval}{Seconds between status checks (default: 5).}

\item{max_wait_seconds}{Maximum wait time for job completion (default:
3600).}

\item{timeout}{HTTP request timeout in seconds (default: 600).}
}
\value{
A list with elements:
\describe{
\item{file_path}{Input file path or URL.}
\item{job_id}{The submitted job ID.}
\item{output_dir}{Output directory path.}
\item{markdown_files}{Paths to saved Markdown files.}
\item{page_count}{Number of pages processed.}
}
}
\description{
This is the main entry point for the PaddleOCR cloud API. Submit a local
file (image or document) or a URL, and the function will handle job
submission, polling, result retrieval, and file saving automatically.
}
\examples{
\dontrun{
# OCR a local image
result <- paddle_ocr("document.png")

# OCR a URL
result <- paddle_ocr("https://example.com/document.jpg")

# With custom options
result <- paddle_ocr(
  file_path = "table.png",
  use_chart_recognition = TRUE,
  output_dir = "my_output"
)
}
}
